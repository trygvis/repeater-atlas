services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: nrrl_repeater_atlas
      POSTGRES_USER: nrrl
      POSTGRES_PASSWORD: nrrl
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
