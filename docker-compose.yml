services:
  postgres:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_DB: repeater_atlas
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
