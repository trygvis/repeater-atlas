---
id: ra-428d
status: open
deps: []
links: []
created: 2026-04-15T10:11:48Z
type: task
priority: 2
assignee: Trygve Laugstøl
---

# Fix HTML element order on map view

After a change where the right hand pane became fixed, parts of the map is not
seen anymore. This is a problem when finding the nearest repeaters.

Tasks:

- Reorganize the element structure so that there is a tree structure like this:
  ```
  body
    - map
    - side-pane
      - pane-body
      - icon-column
  ```
- Check if the best HTML elements are used for each section. It might be nice to
  use `div` element to set the sizes of the map/pane and then use PicoCSS stuff
  on the inside.
- Update relevant docs to use "side pane" instead of "left/right pane". The
  details of the icon-column/body is not significant in the documents.
