---
id: ra-073d
status: closed
deps: []
links: []
created: 2026-03-30T06:13:46Z
type: task
priority: 2
assignee: Trygve Laugstøl
---

# Logging connection manager improvements

- Move the LoggingConnectionManager to its own module. It should take an
  underlying connection as its argument instead of doing the init itself.
- Move most/the rest(?) of the code in db_pool into a PgTlConnectionManager so
  that it is a generic, reusable tls wrapper.
- Change src/lib.rs to compose the tls and logging managers.

## Notes

**2026-04-02T06:34:35Z**

Refactored db_pool into two focused modules and merged plumbing into lib.rs.
Moved TLS setup into pg_tls_connection_manager.rs as PgTlsConnectionManager.
Moved LoggingConnectionManager and LoggedConnection into
logging_connection_manager.rs; it now takes an AsyncDieselConnectionManager as
its argument instead of constructing it internally, preserving correct
is_valid/has_broken delegation. Removed db_pool.rs entirely — AppPool type alias
and build_manager() now live in lib.rs. All callers updated.
